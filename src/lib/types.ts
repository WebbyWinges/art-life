export type TPreviewHeader = {
  howDoesItWork: string;
  specialisation: string;
  button: string;
};

export type TAuthHeader = {
  login: string;
  registration: string;
};

export type TUserHeader = {
  myProjects: string;
  profile: string;
  settings: string;
  logout: string;
};
